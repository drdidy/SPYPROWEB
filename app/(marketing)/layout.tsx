import { MarketingFrame } from "@/components/rebirth/MarketingFrame";

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <MarketingFrame>{children}</MarketingFrame>;
}
