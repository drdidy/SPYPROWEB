
## Decision Slate Panel Chrome

The top verdict card uses one outer container. Interior columns are separated by spacing and alignment, not nested bordered cards. Charts inside the verdict card render frameless when `slate_verdict_chrome` is enabled so the verdict copy, recommended-path chart, and next-event rail share the same treatment.

Scorecard content belongs inside the metric strip. Entry-cost status is a metric tile, not a standalone explanatory strip.
